# Правила репозитория

Тактический авто-баттлер: игрок расставляет героев с отрядами по сетке и запускает
бой, в который не может вмешаться. Весь геймплей — в подготовке.

Отвечай по-русски. Термины, имена файлов и код — на английском.

## ДОКУМЕНТАЦИЯ
Канонические (спека заказчика, источник правды):
- `docs/00_START_HERE.md` — рамка проекта, право, навигация
- `docs/01_RESEARCH_original.md` — реконструкция оригинала
- `docs/02_GDD.md` — дизайн: формулы боя, матрица контров, прогрессия, экономика
- `docs/03_TECH_ARCHITECTURE.md` — стек, слои, детерминизм, производительность
- `docs/04_ROADMAP.md` — фазы, Definition of Done, риски
- `docs/05_SKILLS_AND_TEAM.md` — навыки и роли

Рабочие (дополняют спеку, не заменяют):
- `docs/00_MERGE_REPORT.md` — ⚠️ сверка спеки с ресерчем: 3 бага в 03_TECH §4.2 и 5 конфликтов
- `docs/01a_RESEARCH_ADDENDUM.md` — чего нет в 01_RESEARCH: формула брони, лейновая изоляция, константы
- `docs/DECISIONS.md` — журнал ADR
- `docs/TASKS.md` — реестр задач, DoD, критический путь
- `docs/QUESTIONS.md` — открытые вопросы
- `docs/BENCH_P0-07.md` — измеренная стоимость fixed-point

⚠️ 03_TECH §4.2 содержит три нерабочих примера кода (fpMul теряет точность,
Rng не PCG32, nextBelow даёт modulo bias). Правильные реализации — в
packages/sim, разбор — в 00_MERGE_REPORT.md. Не копируй код из §4.2.

Не выдумывай цифры и правила, уже описанные в документах — сверяйся с ними.
Если документ противоречит задаче, скажи об этом, не изобретай молча.

## ИНТЕЛЛЕКТУАЛЬНАЯ СОБСТВЕННОСТЬ
IP жив. МОЖНО: воспроизводить механики, системы, формулы, кривые прогрессии.
НЕЛЬЗЯ и не предлагай: декомпиляцию APK/IPA, извлечение кода или ассетов;
имена героев оригинала; топонимы; сюжет; название оригинала в продукте.
Если задача упирается в этот список — скажи прямо и предложи замену.

## СТЕК
TypeScript strict · PixiJS v8 · Vite · Vitest · Zod · pnpm workspaces
`packages/sim` не импортирует ничего: ни другие пакеты, ни браузерные, ни Node API.

## ИНВАРИАНТЫ ДЕТЕРМИНИЗМА — нарушение = критический баг
В `packages/sim` ЗАПРЕЩЕНО: float в игровой логике (только Fixed Q16.16),
`Math.random` (только seeded PCG32), `Date.now`/`performance.now`,
`Math.sin/cos/sqrt/pow` (только предвычисленные таблицы), async/промисы/таймеры,
порядок из `Object.keys`/`Set`/`Map`, любые внешние API.
ОБЯЗАТЕЛЬНО: фиксированный тик 1/30 с; обход сущностей по возрастанию entityId;
один RNG-поток с фиксированным порядком потребления; спавн и смерти — пакетом
в конце тика.

Дополнительно, выведено из замеров и ресерча:
- Никаких классов и обёрток над `Fx` — обёртка стоит вдвое дороже свободных функций
- Никаких генераторов с `yield` в горячем цикле
- `Set`/`Map` не используются как хранилище ячеек пространственного хэша
- Все сортировки — полный порядок с `entityId` последним тай-брейкером

## ДИЗАЙН-СТОЛПЫ — проверяй каждую задачу на соответствие
1. Формация — это геймплей. Исход боя определяется подготовкой. Ручные действия
   в бою дают КРАЙ, а не победу. Ручного таргетинга нет — игрок не выбирает цели
   и не двигает юнитов. (Редакция от 2026-08-13, см. DECISIONS.md ADR-002.
   Жёсткое ограничение: любой бой кампании проходится без единого каста.)
2. Бой читаем: причина поражения понятна за 5 секунд.
3. Игра не умирает: offline-first, бой всегда считается на клиенте.
4. Экономика не врёт: объявленные шансы = реальные, pity соблюдается.

## ЗАКРЫТЫЕ РЕШЕНИЯ (DECISIONS.md) — не переоткрывай без причины
- Сетка: 20 слотов = 5 лейнов × 4 ранга глубины. Отсюда бюджет 840 юнитов
- Формула урона: процентная, raw × K/(K+DEF), K = 400 + 15×level. Пол урона max(1,..)
- Стек баффов МУЛЬТИПЛИКАТИВНЫЙ: +36% и -36% дают -13%, не 0%
- Лейновая изоляция: melee ищет цель в своём лейне, ranged стреляет сквозь лейны
- Рост статов: × (1 + 0.05 × (lvl-1)) — та же кривая, что у SKILL_BUDGET
- Гача: soft pity с 30 ролла, hard pity 50, базовый шанс 4★ = 2%
- 3 ручных каста с кулдаунами + авто-политика каста (она же защита в спарринге/PvP)
- PvP в v1 нет; «Спарринг» закрывает потребность; реплей готов к серверной проверке

## КЛЮЧЕВАЯ БАЛАНСНАЯ ФОРМУЛА
procChance × skillDamage = SKILL_BUDGET(stars, lvl) = BASE[stars] × (1 + 0.05×lvl)
BASE = {1★:120, 2★:200, 3★:300, 4★:400, 5★:520}
skillDamage НИКОГДА не задаётся вручную — только вычисляется.
Дизайнер задаёт только procChance (0.3 / 0.4 / 0.5 / 0.6).

Подтверждено ресерчем: у шести 4★-героев оригинала произведение
procChance × skillDamage держится в коридоре 380–415, разброс ±4.5%.

## БЮДЖЕТЫ
840 юнитов в бою. Тик симуляции ≤ 4 мс (p99 ≤ 10 мс). 60 fps на iPhone SE 2020.
Поиск целей — только через пространственную хэш-сетку, никогда O(n²).
Первая загрузка ≤ 60 МБ (оригинал разбух до 987 МБ, ремейк 2025 — до 2.5 ГБ).

## КАК РАБОТАТЬ
Задача = вход → выход → инварианты → тесты. Тесты пишем до реализации.
Если спека неполна — задай один уточняющий вопрос и предложи разумный дефолт.
Предупреждай, когда задача нарушает детерминизм, столпы или выходит за скоуп.
Предлагай упрощение вместо расширения.
Меняй по одному файлу за раз, решения объясняй коротко.

## ТЕКУЩИЙ СТАТУС
Фаза 1 ЗАКРЫТА. Фаза 2 почти закрыта. 64 теста зелёные, CI зелёный.

packages/sim — ядро:
- fixed.ts (Q16.16 + Q24.8 + isqrt), rng.ts (PCG32, сверен с эталонными
  векторами), trig.ts (BAM-углы + четвертьволновая таблица)
- world.ts (SoA), spatial.ts (counting-sort бакеты), hash.ts (FNV-1a посубсистемно)
- 8 классов, матрица контров 8x8, блокировка павизами, сплэш магов, лечение
- events.ts — буфер боевых событий для рендера (урон/крит/контр/сплэш/лечение/блок)
- battle.ts — createBattle/stepBattle/finishBattle + runBattle поверх них
- golden-реплеи, fuzz на 200 боёв, барьер детерминизма тестом

packages/render — PixiJS v8:
- атлас силуэтов (форма = класс, цвет = сторона), интерполяция между тиками
- полоски HP героев, всплывающие цифры с цветовой кодировкой
- индикаторы контра и блокировки (02_GDD §7.1)
- автокадрирование камеры по живым юнитам

packages/app — экран формации 5x4 (drag-and-drop + тап), автозаполнение,
скорости 1x/2x/4x, скип, экран результата меч/сердце/щит + таблица героев.

packages/tools — ASCII-визуализатор (`pnpm battle`), скриншот-тесты Playwright.

Измерено: 840 юнитов = 0.100 мс/тик при бюджете 4 мс.

ДОЛГ ФАЗЫ 4 (балансный тулинг) — руками не трогать, нужен batch-sim:
- маг ломает бой: 15562 урона при нуле полученного, x8 к следующему герою
- лучники на лезвии ножа: baseAtk 88->76 переворачивает винрейт с 0.72 до 0.02
- смешанный состав доминирует над моно (винрейт ~1.0)

НЕ СДЕЛАНО в фазе 2: плейтест на 5 людях (формальный гейт фазы),
симуляция в Web Worker (APP-01).
