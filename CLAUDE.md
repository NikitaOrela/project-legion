# Правила репозитория

Тактический авто-баттлер: игрок расставляет героев с отрядами по сетке и запускает
бой, в который не может вмешаться. Весь геймплей — в подготовке.

Отвечай по-русски. Термины, имена файлов и код — на английском.

## ДОКУМЕНТАЦИЯ
Канонические (спека заказчика, источник правды):
- `docs/00_START_HERE.md` — рамка проекта, право, навигация
- `docs/01_RESEARCH_original.md` — реконструкция оригинала
- `docs/02_GDD.md` — дизайн: формулы боя, матрица контров, прогрессия, экономика
- `docs/03_TECH_ARCHITECTURE.md` — стек, слои, детерминизм, производительность
- `docs/04_ROADMAP.md` — фазы, Definition of Done, риски
- `docs/05_SKILLS_AND_TEAM.md` — навыки и роли

Рабочие (дополняют спеку, не заменяют):
- `docs/00_MERGE_REPORT.md` — ⚠️ сверка спеки с ресерчем: 3 бага в 03_TECH §4.2 и 5 конфликтов
- `docs/01a_RESEARCH_ADDENDUM.md` — чего нет в 01_RESEARCH: формула брони, лейновая изоляция, константы
- `docs/DECISIONS.md` — журнал ADR
- `docs/TASKS.md` — реестр задач, DoD, критический путь
- `docs/QUESTIONS.md` — открытые вопросы
- `docs/BENCH_P0-07.md` — измеренная стоимость fixed-point
- `docs/01b_REBORN_MECHANICS.md` — формулы и таргетинг ремейка 2025
- `docs/01c_REBORN_SYSTEMS.md` — системы, экономика, мета ремейка
- `docs/01d_REBORN_DEEP.md` — скиллы, звёзды, сакрамент, резонанс, гача
- `docs/01e_REBORN_NUMBERS.md` — ⚠️ ЧИСЛА: статы 8 классов, формулы, деревья
- `docs/BALANCE_01.md` … `_05` — замеры баланса, читать последний
- `docs/06_SKILLS_BRIEF.md` — бриф на систему скиллов, передача смены
- `docs/07_SKILLS_RESEARCH.md` — ⚠️ разбор скиллов 257 героев ремейка
- `docs/data/07a_SKILLS_CORPUS.md` — сырьё под 07, дословная выгрузка

⚠️ 03_TECH §4.2 содержит три нерабочих примера кода (fpMul теряет точность,
Rng не PCG32, nextBelow даёт modulo bias). Правильные реализации — в
packages/sim, разбор — в 00_MERGE_REPORT.md. Не копируй код из §4.2.

Не выдумывай цифры и правила, уже описанные в документах — сверяйся с ними.
Если документ противоречит задаче, скажи об этом, не изобретай молча.

## ИНТЕЛЛЕКТУАЛЬНАЯ СОБСТВЕННОСТЬ
IP жив. МОЖНО: воспроизводить механики, системы, формулы, кривые прогрессии.
НЕЛЬЗЯ и не предлагай: декомпиляцию APK/IPA, извлечение кода или ассетов;
имена героев оригинала; топонимы; сюжет; название оригинала в продукте.
Если задача упирается в этот список — скажи прямо и предложи замену.

## СТЕК
TypeScript strict · PixiJS v8 · Vite · Vitest · Zod · pnpm workspaces
`packages/sim` не импортирует ничего: ни другие пакеты, ни браузерные, ни Node API.

## ИНВАРИАНТЫ ДЕТЕРМИНИЗМА — нарушение = критический баг
В `packages/sim` ЗАПРЕЩЕНО: float в игровой логике (только Fixed Q16.16),
`Math.random` (только seeded PCG32), `Date.now`/`performance.now`,
`Math.sin/cos/sqrt/pow` (только предвычисленные таблицы), async/промисы/таймеры,
порядок из `Object.keys`/`Set`/`Map`, любые внешние API.
ОБЯЗАТЕЛЬНО: фиксированный тик 1/30 с; обход сущностей по возрастанию entityId;
один RNG-поток с фиксированным порядком потребления; спавн и смерти — пакетом
в конце тика.

Дополнительно, выведено из замеров и ресерча:
- Никаких классов и обёрток над `Fx` — обёртка стоит вдвое дороже свободных функций
- Никаких генераторов с `yield` в горячем цикле
- `Set`/`Map` не используются как хранилище ячеек пространственного хэша
- Все сортировки — полный порядок с `entityId` последним тай-брейкером

## ДИЗАЙН-СТОЛПЫ — проверяй каждую задачу на соответствие
1. Формация — это геймплей. Исход боя определяется подготовкой. Ручные действия
   в бою дают КРАЙ, а не победу. Ручного таргетинга нет — игрок не выбирает цели
   и не двигает юнитов. (Редакция от 2026-08-13, см. DECISIONS.md ADR-002.
   Жёсткое ограничение: любой бой кампании проходится без единого каста.)
2. Бой читаем: причина поражения понятна за 5 секунд.
3. Игра не умирает: offline-first, бой всегда считается на клиенте.
4. Экономика не врёт: объявленные шансы = реальные, pity соблюдается.

## ЗАКРЫТЫЕ РЕШЕНИЯ (DECISIONS.md) — не переоткрывай без причины
- Сетка: 20 слотов = 5 лейнов × 4 ранга глубины. Отсюда бюджет 840 юнитов
- ⚠️ ИНВАРИАНТ ГЕОМЕТРИИ (ADR-013, закрыт тестом): rankDepth × (ranks−1) <
  дальность самого дальнобойного класса. Иначе задний ранг мёртв — юнит туда
  ставится, но до боя не достаёт никогда. rankDepth задаёт ЦЕНУ глубины:
  чем теснее ранги, тем бесплатнее ставить стрелков в тыл
- Формула урона: ATK²/(ATK+DEF), контр применяется ПОСЛЕ митигации, крит после
  контра. Пол урона max(1,..). ADR-009 заменил прежнюю K/(K+DEF)
- Семейство X/(X+Y) (ADR-011): попадание = Acc/(Acc+Eva), крит = Crit/(Crit+Ten),
  крит-множитель у атакующего. Порядок бросков — попадание, крит, разброс, и все
  три делаются ВСЕГДА, иначе число обращений к RNG зависит от исхода
- Фронт ограничен: одну цель держат ≤3 ближних и ≤6 дальних юнитов. Это перевод
  боя из квадратичного закона Ланчестера в линейный — не трогать без замера
- Матрица контров РАЗРЕЖЁННАЯ, ровно один экстремум: Archer→Skyrider +120%
  (ADR-012). Копьё против конницы — +40%, как в первоисточнике
- Таргетинг с классовыми приоритетами (PREF_TARGET) — контр не множитель, а поиск
- Стек баффов МУЛЬТИПЛИКАТИВНЫЙ: +36% и -36% дают -13%, не 0%
- Лейновая изоляция: melee ищет цель в своём лейне, ranged стреляет сквозь лейны
- Рост статов: × (1 + 0.05 × (lvl-1)) — та же кривая, что у SKILL_BUDGET
- Гача: soft pity с 30 ролла, hard pity 50, базовый шанс 4★ = 2%
- 3 ручных каста с кулдаунами + авто-политика каста (она же защита в спарринге/PvP)
- PvP в v1 нет; «Спарринг» закрывает потребность; реплей готов к серверной проверке

## ⚠️ КЛЮЧЕВАЯ БАЛАНСНАЯ ФОРМУЛА — ОПРОВЕРГНУТА, НЕ ПРИМЕНЯТЬ (ADR-016)
Формула `(procChance / skillPeriodSec) × skillDamage = SKILL_BUDGET` держалась
на шести героях. Разбор 257 героев ремейка (`07_SKILLS_RESEARCH` §1): у 19
героев с проком произведение гуляет 6…150, у 104 с таймером `урон/период` —
0.8…240. Единой балансной валюты в первоисточнике нет.

Правильная форма, вес контроля ещё не выведен:
  шанс × урон × число_целей × качество_выбора_цели + вес_нечисловых_эффектов
Урон в долях максимума HP — отдельная валюта: он игнорирует защиту.

Что из замысла уцелело — ДИСКРЕТНОСТЬ. Урон НИКОГДА не вбивается руками, но
и не вычисляется: он ВЫБИРАЕТСЯ из пула. Шанс ∈ {10,25,30,40,50,60,65}%,
урон — пять пресетов вида 50(200)%, период ∈ {2…15 с}, рост до 7 уровня ×2.5
в 70% случаев. Медиана произведения 80 — якорь первого скилла.
Период обязателен: без него «через N атак» делает скорость атаки
самоусиливающимся статом — она разгоняет и урон, и частоту скиллов сразу.

Правило шире случая: число из источника имеет силу ровно того охвата, на
котором проверено. Шесть героев — гипотеза, а не подтверждение.

## БЮДЖЕТЫ
840 юнитов в бою. Тик симуляции ≤ 4 мс (p99 ≤ 10 мс). 60 fps на iPhone SE 2020.
Поиск целей — только через пространственную хэш-сетку, никогда O(n²).
Первая загрузка ≤ 60 МБ (оригинал разбух до 987 МБ, ремейк 2025 — до 2.5 ГБ).

## КАК РАБОТАТЬ
Задача = вход → выход → инварианты → тесты. Тесты пишем до реализации.
Если спека неполна — задай один уточняющий вопрос и предложи разумный дефолт.
Предупреждай, когда задача нарушает детерминизм, столпы или выходит за скоуп.
Предлагай упрощение вместо расширения.
Меняй по одному файлу за раз, решения объясняй коротко.

## ТЕКУЩИЙ СТАТУС
Фаза 1 ЗАКРЫТА. Фаза 2 почти закрыта. Фаза 4 идёт: боевая модель пересобрана
(ADR-009). 64 теста зелёные.

packages/sim — ядро:
- fixed.ts (Q16.16 + Q24.8 + isqrt), rng.ts (PCG32, сверен с эталонными
  векторами), trig.ts (BAM-углы + четвертьволновая таблица)
- world.ts (SoA), spatial.ts (counting-sort бакеты), hash.ts (FNV-1a посубсистемно)
- 9 классов со статами, выведенными из ремейка (01e), разрежённая матрица
  контров, классовые приоритеты таргетинга, ограничение фронта, Hold Ground
  у павизы, сплэш мага как скилл по таймеру
- events.ts — буфер боевых событий для рендера
- battle.ts — createBattle/stepBattle/finishBattle + runBattle поверх них
- golden-реплеи, fuzz на 200 боёв, барьер детерминизма тестом

packages/render — PixiJS v8: атлас силуэтов, интерполяция, полоски HP,
всплывающие цифры, индикаторы контра и блокировки, автокадрирование.

packages/app — экран формации 5x4, скорости 1x/2x/4x, скип, экран результата.

packages/tools — ASCII-визуализатор (`pnpm battle`), батч-симулятор (`pnpm sim`),
скриншот-тесты Playwright.

Измерено: 840 юнитов = 1.02 мс/тик, p99 2.99 мс при бюджете 4 / 10 мс.
Бой длится 37.7 с при целевых 30–60. Вклад в урон: лучник 21.1%, арбалет 17.8%,
маг 16.9%, копьё 13.0%, пехота 12.7%, кавалерия 11.3%, павиза 7.3%.
Лечение — 13.5% от всего урона, считается отдельной строкой.

ТИК ОДНОВРЕМЕНЕН (ADR-010) — не откатывать в «применять сразу»:
урон, лечение и шаги движения копятся и раскрываются в конце прохода.
Иначе команда с меньшими entityId бьёт и ходит раньше, и исход решает номер
в массиве, а не состав. Геометрия тоже симметрична к отражению: сдвиг
применяется к РАЗНОСТИ координат и по модулю (distSqUnits, deltaUnits),
центроиды округляются к ближайшему, а не отсекаются к нулю.

DoD ФАЗЫ 4 (ADR-014 — главная метрика сменилась второй раз, читай почему):
- ⭐ СОСТАВОВ БЕЗ УПРАВЫ = 0 — ДОСТИГНУТО (0 из 11)
  Жёсткие контры ПО ОПРЕДЕЛЕНИЮ делают матчап предрешённым. Требовать
  неопределённости, имея контры, — значит требовать, чтобы контры не работали.
  Болезнь меты — не предрешённость пар, а состав, которому нечего
  противопоставить. `pnpm sim` печатает лучший ответ на каждый состав
- матчапов с непредрешённым исходом ≥ 40% — сейчас 30%, метрика справочная
- перекос сторон не более 15 п.п. — сейчас ~50, разобрано в ADR-010
Все метрики печатает `pnpm sim`.

⚠️ ЧЕТЫРЕ РАЗА ПОДРЯД проблема оказывалась не в классе, а в РАССТАНОВКЕ
архетипа: лекари в тылу вне дальности, маги в тылу вне дальности, налётчики за
медленными павизами. Прежде чем трогать числа класса — проверь расстановку:
достаёт ли юнит до боя (rankDepth × rank ≤ range) и не мешает ли свой фронт.

⚠️ batch-sim нормализует архетипы ПО ЧИСЛУ СЛОТОВ (6 слотов × 7 бойцов), а не
по числу бойцов. Слот даёт героя И отряд, герой сильнее миньона — при равной
численности больше слотов означает больше героев, и сравнивается не состав.
Дважды подряд инструмент измерения оказывался источником главного вывода:
прежде чем объяснять неожиданный результат механикой, проверь, что меряешь.

ДОЛГ, ПОДТВЕРЖДЁННЫЙ ЗАМЕРОМ (BALANCE_04 + ADR-013):
- глубина подорожала (rankDepth 50→60), глубокие построения просели с 85/84 до
  74/69. Обратная сторона: дальний бой хуже прикрывает фронт, и вырос быстрый
  ближний бой — моно-налётчик 89%, моно-кавалерия 77%. Это следующая работа
- ТРИЖДЫ подряд архетип в batch-sim был собран с юнитом вне его дальности
  (лекари, маги). Проверять `rankDepth × rank ≤ range` для каждого слота
- уклонение налётчика 30 вместо измеренных 50 — временно. Вернуть вместе с
  экипировкой (точность) и скиллами (неуклоняемы), которые его уравновешивают
- перекос сторон 45 п.п. на почти-зеркальных парах. Точная зеркальная симметрия
  недостижима (floor не коммутирует с отражением) — для PvP решать на уровне
  матча: играть обе стороны или сравнивать по остатку HP, а не по факту победы
- кап урона скиллов по рядовым РЕАЛИЗОВАН (SKILL_VS_SOLDIER_PCT), но площадь у
  базовой атаки мага так и не вернулась: замер показал ухудшение по всем
  метрикам (ADR-015). Кап ждёт настоящих скиллов героев
- ⚠️ БАЛАНС МЕРИТСЯ В ОДНОЙ ТОЧКЕ, а надо в двух (`07` §11). В первоисточнике
  мета переворачивается прокачкой: летун решает раннюю игру, а к максимальным
  уровням лучник сносит его элементарно. Наш моно-налётчик 89% — возможно, не
  долг, а норма ранней стадии. Проверяется прогоном `pnpm sim` на базовых и на
  прокачанных статах; пока прогоняется только базовый
- ⚠️ ДВА СЛУЧАЯ ПОДРЯД, когда находка первоисточника ухудшала нашу модель:
  уклонение налётчика 50 и площадь мага. Там они уравновешены системами и
  ролями, которых у нас нет. Верность источнику — не аргумент против замера

НЕ СДЕЛАНО в фазе 2: плейтест на 5 людях (формальный гейт фазы),
симуляция в Web Worker (APP-01).
